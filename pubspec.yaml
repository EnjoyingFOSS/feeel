name: feeel
description: A simple home workout/exercise app that respects your privacy

version: 2.4.1+4241

publish_to: none

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: ^0.18.0
  
  http: ^1.0.0
  flutter_tts: ^3.6.3
  wakelock: any
  path_provider: ^2.0.14
  shared_preferences: ^2.1.0
  audioplayers: ^5.0.0
  flutter_local_notifications: ^13.0.0
  flutter_native_timezone: ^2.0.0
  timezone: ^0.9.1
  url_launcher: ^6.1.10
  package_info_plus: ^6.0.0
  auto_size_text: ^3.0.0
  flutter_markdown: ^0.6.14
  flutter_staggered_animations: ^1.1.1
  xdg_directories: ^0.2.0
  flutter_speed_dial: ^6.2.0
  diacritic: ^0.1.3
  flutter_riverpod: ^2.3.4
  table_calendar: ^3.0.9
  share_plus: ^7.0.0
  file_picker: ^5.2.9
  dynamic_color: ^1.6.3 # for Material You theming
  device_info_plus: ^8.2.0 # for checking support for Material You theming only
  archive: ^3.3.7
  handy_window: ^0.3.1

  # api
  chopper: ^6.1.1
  json_annotation: ^4.8.0

  #drift
  drift: ^2.7.0
  sqlite3_flutter_libs: ^0.5.0
  path: ^1.8.2
  freezed_annotation: ^2.2.0
  sqlite3: ^1.11.0

dependency_overrides:
  intl: 0.18.0

dev_dependencies:
  # flutter_test:
  #   sdk: flutter
  # integration_test:
  #   sdk: flutter
  # flutter_driver:
  #   sdk: flutter

  # api
  chopper_generator: ^6.0.0
  json_serializable: ^6.6.1
  swagger_dart_code_generator: ^2.10.4

    # drift
  drift_dev: ^2.7.0
  build_runner: ^2.3.3
  flutter_lints: ^2.0.1
  freezed: ^2.3.2


flutter:
  uses-material-design: true
  generate: true

  assets:
    - assets/images/
    - assets/exercise_images/
    - assets/exercise_thumbs/
    - assets/json/
    - assets/json_supplements/
    - assets/sounds/
    - README.md
